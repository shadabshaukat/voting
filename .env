# Environment variables for the Voting & Trivia application
# -----------------------------------------------------------
# Copy this file to .env (if not already) and edit the values as needed.
# The application loads these variables automatically via python‑dotenv
# (uv run will pick them up when the process starts).

# Database connection URL (PostgreSQL 17)
DATABASE_URL=postgresql+psycopg2://postgres:RAbbithole1234##@10.10.1.83:5432/postgres

# JWT configuration
JWT_SECRET_KEY=supersecretkey
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# Optional: override the host and port the FastAPI server listens on
# UVICORN_HOST=0.0.0.0
# UVICORN_PORT=8000
